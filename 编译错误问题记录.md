# Vibe Kanban 编译错误问题记录

**目的**: 记录所有遇到过的编译错误和解决方案，避免重复犯错  
**更新日期**: 2026-01-25

---

## 🚨 关键问题清单

### 1. ✅ 大文件提交问题

**错误表现**:
```
GitHub 警告: 提交了 75MB 二进制文件
```

**原因**:
- 提交了 `vibe-kanban.exe` (64MB)
- 提交了 `rustup-init.exe` (11MB)

**解决方案**:
```bash
git rm --cached vibe-kanban.exe rustup-init.exe
echo "*.exe" >> .gitignore
git commit -m "chore: remove large binary files"
```

**预防措施**:
- ✅ 永不提交二进制文件到 Git
- ✅ 检查 `.gitignore` 是否正确配置
- ✅ 提交前运行 `git status` 检查大文件

---

### 2. ✅ SQLx 离线模式缺失

**错误表现**:
```
error: set `DATABASE_URL` to use query macros online
   --> crates/db/src/models/task_session.rs:37:9
```

**原因**:
- GitHub Actions workflow 缺少 `SQLX_OFFLINE=true`
- SQLx 默认尝试连接数据库验证查询

**解决方案**:
在 `.github/workflows/pre-release.yml` 的所有后端构建步骤中添加：
```yaml
env:
  SQLX_OFFLINE: true  # 关键配置
```

**涉及文件**:
- `.github/workflows/pre-release.yml` (Linux 构建)
- `.github/workflows/pre-release.yml` (macOS 构建)
- `.github/workflows/pre-release.yml` (Windows 构建)

**预防措施**:
- ✅ 独有功能使用运行时查询，不使用查询宏
- ✅ 检查 workflow 是否包含 `SQLX_OFFLINE=true`
- ✅ 不要使用 `sqlx::query_as!` 等编译时宏

---

### 3. ✅ SQLx 查询缓存缺失

**错误表现**:
```
error: `SQLX_OFFLINE=true` but there is no cached data for this query
   --> crates/db/src/models/task_session.rs:37:9
```

**原因**:
- 独有功能使用 `sqlx::query_as!` 宏
- 没有对应的 `.sqlx/query-*.json` 缓存文件
- 上游不会有我们独有功能的缓存

**解决方案**:
将所有 SQLx 查询宏改为运行时查询：

**错误写法**:
```rust
sqlx::query_as!(
    TaskSession,
    r#"SELECT id AS "id!: Uuid",
              task_id AS "task_id!: Uuid"
       FROM task_sessions
       WHERE id = $1"#,
    id
)
```

**正确写法**:
```rust
sqlx::query_as::<_, TaskSession>(
    r#"SELECT id,
              task_id
       FROM task_sessions
       WHERE id = ?"#
)
.bind(id)
```

**修改的文件**:
1. `crates/db/src/models/task_session.rs` - 6 个查询
2. `crates/db/src/models/project.rs` - `find_by_name` 查询
3. `crates/db/src/models/repo.rs` - `update_path` 查询
4. `crates/db/src/models/tag.rs` - `find_by_tag_name` 查询

**预防措施**:
- ✅ 独有功能永远使用运行时查询
- ✅ 避免使用任何 `sqlx::*!` 宏
- ✅ 使用 `sqlx::query_as::<_, Type>` 形式

---

### 4. ✅ 缺少 walkdir 依赖

**错误表现**:
```
error[E0433]: failed to resolve: use of unresolved module or unlinked crate `walkdir`
   --> crates/services/src/services/session_exporter.rs:174:23
```

**原因**:
- 上游使用了 `walkdir` crate
- 但在 `Cargo.toml` 中忘记声明依赖

**解决方案**:
在 `crates/services/Cargo.toml` 中添加：
```toml
walkdir = "2.5"
```

**位置**: 放在 `sqlx` 和 `chrono` 之间

**预防措施**:
- ⚠️ 这是上游的问题，无法预防
- ✅ 记住这个错误，下次添加类似功能时检查依赖

---

### 5. ✅ session_exporter 类型错误

**错误表现**:
```
error[E0308]: mismatched types
   --> crates/services/src/services/session_exporter.rs:129:16
    |
129 |         if let Some(branch) = &workspace.branch {
    |                ^^^^^^^^^^^^   ----------------- this expression has type `&std::string::String`
    |                |
    |                expected `String`, found `Option<_>`

error[E0308]: mismatched types
   --> crates/services/src/services/session_exporter.rs:192:81
    |
192 |             if let Some(task_id) = self.extract_task_id_from_path(&session_dir, &path) {
    |                                         -------------------------               ^^^^ expected `&PathBuf`, found `&&Path`
```

**原因**:
1. `workspace.branch` 是 `Option<Option<String>>` (双重嵌套 Option)，而不是 `Option<String>`
2. WalkDir 返回的 `path` 是 `&Path`，但函数期望 `&PathBuf`

**解决方案**:
1. 双重嵌套 Option 使用 `if let Some(Some(branch))` 模式
2. 使用 `path.to_path_buf()` 将 `&Path` 转换为 `PathBuf`

**错误写法**:
```rust
if let Some(branch) = &workspace.branch {
    // branch 的类型是 &Option<String>，不是 &String
}
if let Some(task_id) = self.extract_task_id_from_path(&session_dir, &path) {
    // &path 是 &&Path，不是 &PathBuf
}
```

**正确写法**:
```rust
if let Some(Some(branch)) = &workspace.branch {
    md.push_str(&format!("- **Branch**: {}\n", branch));
}
let path_buf = path.to_path_buf();
if let Some(task_id) = self.extract_task_id_from_path(&session_dir, &path_buf) {
    // ...
}
```

**预防措施**:
- ⚠️ 这是上游的类型定义问题，无法预防
- ✅ 注意区分 `Option<Option<T>>` 和 `Option<T>`
- ✅ 注意区分 `&Path`、`PathBuf`、`&PathBuf`
- ✅ WalkDir 的 `entry.path()` 返回 `&Path`，需要用 `.to_path_buf()` 转换

---

## 📋 编译前检查清单

### 代码修改后必须检查

- [ ] 检查是否使用了 `sqlx::*!` 宏
- [ ] 如果使用了宏，改为运行时查询
- [ ] 检查是否引入了新的外部 crate
- [ ] 检查 `Cargo.toml` 是否包含所有依赖
- [ ] 检查 `.gitignore` 是否正确
- [ ] 检查是否有大文件被追踪

### 提交前检查

- [ ] 运行 `npm run lint` (前端)
- [ ] 运行 `npm run check` (前端)
- [ ] 运行 `cargo check` (后端)
- [ ] 检查 `git status` 中是否有大文件

### 推送前检查

- [ ] 确认所有依赖已声明
- [ ] 确认 workflow 配置正确
- [ ] 确认 SQLX_OFFLINE=true 已设置

---

## 🔧 快速修复命令

### 如果遇到 SQLx 错误

```bash
# 1. 检查是否使用了查询宏
grep -r "sqlx::query_as!\|sqlx::query!\|sqlx::query_scalar!" crates/db/src/

# 2. 修改为运行时查询
# 从 sqlx::query_as!(Type, "SELECT ... WHERE id = $1", id)
# 改为 sqlx::query_as::<_, Type>("SELECT ... WHERE id = ?").bind(id)
```

### 如果遇到缺少依赖错误

```bash
# 1. 查看使用的 crate
grep -r "use " | grep "walkdir\|其他crate"

# 2. 添加到对应的 Cargo.toml
# 添加 crate_name = "version"
```

### 如果遇到大文件警告

```bash
# 1. 查找大文件
find . -type f -size +50M -not -path "./.git/*"

# 2. 移除追踪
git rm --cached <file>

# 3. 更新 .gitignore
echo "*.exe" >> .gitignore
```

---

## 📚 经验教训

### 核心原则

1. **CI/CD 环境必须离线编译**
   - 使用 `SQLX_OFFLINE=true`
   - 避免依赖数据库连接

2. **独有功能避免编译时检查**
   - 不使用 `sqlx::*!` 宏
   - 使用运行时查询 API

3. **永远不提交二进制文件**
   - 配置正确的 `.gitignore`
   - 提交前检查文件大小

4. **依赖声明必须完整**
   - 检查 `Cargo.toml`
   - 包含所有使用的 crate

### 常见陷阱

| 陷阱 | 后果 | 解决方案 |
|------|------|---------|
| 使用查询宏 | CI 编译失败 | 改用运行时查询 |
| 提交二进制文件 | 仓库膨胀 | 添加到 .gitignore |
| 忘记声明依赖 | 编译失败 | 检查并添加到 Cargo.toml |
| 缺少离线模式配置 | CI 编译失败 | 添加 SQLX_OFFLINE=true |

---

## 🎯 最佳实践

### 添加新功能时

1. **后端新增功能**
   ```rust
   // ❌ 不要这样
   sqlx::query_as!(MyType, "SELECT ... WHERE id = $1", id)
   
   // ✅ 要这样
   sqlx::query_as::<_, MyType>("SELECT ... WHERE id = ?")
       .bind(id)
   ```

2. **添加新依赖**
   ```bash
   # 立即添加到 Cargo.toml
   cargo add dependency-name
   ```

3. **提交前检查**
   ```bash
   # 检查大文件
   git ls-files | while read f; do [ -f "$f" ] && [ $(stat -f %s "$f") -gt 52428800 ] && echo "$f"; done
   
   # 检查查询宏
   grep -r "sqlx::.*!" crates/
   ```

---

## 📝 问题追踪

| 日期 | 问题 | 状态 | 提交 |
|------|------|------|------|
| 2026-01-25 | 大文件警告 | ✅ 已解决 | dea77bec |
| 2026-01-25 | SQLx 离线模式 | ✅ 已解决 | efc8ec24 |
| 2026-01-25 | task_session 查询宏 | ✅ 已解决 | 554bcbe0 |
| 2026-01-25 | project/repo/tag 查询宏 | ✅ 已解决 | 649f53ae |
| 2026-01-25 | 缺少 walkdir 依赖 | ✅ 已解决 | 8ab08b6c |
| 2026-01-25 | session_exporter 类型错误 | ✅ 已解决 | 63dbb92e |

---

## ⚠️ 重点记忆

### 永远不要做的事

1. ❌ 使用 `sqlx::query_as!`、`sqlx::query!`、`sqlx::query_scalar!` 宏
2. ❌ 提交 `.exe`、`.dll`、`.so` 等二进制文件
3. ❌ 忘记在 `Cargo.toml` 中声明依赖
4. ❌ 在 CI/CD 中不设置 `SQLX_OFFLINE=true`

### 必须做的事

1. ✅ 使用 `sqlx::query_as::<_, Type>()` 运行时查询
2. ✅ 检查 `.gitignore` 包含 `*.exe`、`*.dll` 等
3. ✅ 使用新依赖前添加到 `Cargo.toml`
4. ✅ workflow 中设置 `SQLX_OFFLINE=true`

---

**记住这些，以后不再犯同样的错误！**

**最后更新**: 2026-01-25  
**维护人**: AI Assistant
